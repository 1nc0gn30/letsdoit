[build]
  command = "vite build"
  publish = "dist"

[dev]
  framework = "vite"
  port = 8888
  publish = "dist"

[functions]
  directory = "netlify/functions"
  external_node_modules = ["@netlify/database", "@netlify/blobs"]
