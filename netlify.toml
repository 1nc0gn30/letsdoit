[build]
  command = "vite build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[dev]
  framework = "vite"
  port = 8888
  publish = "dist"

[functions]
  directory = "netlify/functions"
  external_node_modules = ["@netlify/database"]
